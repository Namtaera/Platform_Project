spring:
  autoconfigure:
    exclude:
      - org.springframework.boot.autoconfigure.web.servlet.error.ErrorMvcAutoConfiguration

  jackson:
    serialization:
      write-dates-as-timestamps: false

  application:
    name: Platform

  datasource:
    url: jdbc:mariadb://100.112.191.94:3306/stock_db
    driver-class-name: org.mariadb.jdbc.Driver
    username: spring_user
    password: "0000"


  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        format_sql: true
        show_sql: true

server:
  address: 0.0.0.0
  port: 8080
  tomcat:
    threads:
      max: 200